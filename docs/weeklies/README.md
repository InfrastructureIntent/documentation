# Weekly Reports

This directory will hold generated cross-repository weekly progress and architecture-drift reports once the local iteration-log aggregation standard is implemented.

The intended source data is repository-local iteration evidence plus GitHub issues, milestones, pull requests, and project state. Weekly reporting should summarize evidence rather than become a second source of truth.